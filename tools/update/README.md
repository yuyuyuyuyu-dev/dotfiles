# Update

A universal tool to update installed command-line tools and package managers.

## Usage

To run the update process locally, execute the following command from the repository root:

```bash
./bin/update
```
