[misc]
[commands]
# custom command はシェルに文字列を渡して終了コードを見るだけで、組み込み step のような
# 存在確認は行われない。未インストールの環境でも 0 で終える必要があるため、ガードを自前で書く。
"Android CLI" = "command -v android > /dev/null || exit 0; android update"
